# Метрики работы Antigravity

Журнал запусков `agy` (правила — в `CLAUDE.md`). Цель — после 10 засчитанных запусков решить, даёт ли Antigravity выигрыш по сравнению с выполнением задач самим Claude.

## Поля

- **Дата** — дата запуска.
- **Задача** — кратко, что поручено, и затронутые файлы.
- **Модель** — модель `agy`.
- **Время** — от запуска до завершения, мин.
- **Оценка Claude** — сколько, по оценке Claude, заняла бы задача у него самого, мин.
- **Результат** — `принято` / `принято с доработкой` / `отклонено` / `не запущен`.
- **Доработка** — что пришлось исправлять Claude и сколько времени это заняло.
- **Засчитан** — `да`, если Antigravity реально выполнил задачу (успешно или нет); `нет`, если запуск не состоялся по внешним причинам.

## Журнал

| # | Дата | Задача | Модель | Время | Оценка Claude | Результат | Доработка | Засчитан |
|---|---|---|---|---|---|---|---|---|
| 0 | 2026-09-11 | Три мелких фикса: `lobby.tsx` (catch), `board.tsx` (скрытая зона), `db/combat.ts` (кавычки в SQL) | gemini-3.8-flash-high | — | ~3 | не запущен | Запуск заблокирован режимом разрешений Claude Code; задачи сделал Claude. Вывод: такие мелкие правки Antigravity поручать невыгодно | нет |

| 1 | 2026-09-11 | Удалить 43 неиспользуемых CSS-класса из `app/globals.css` по проверенному списку (~150 селекторов) | gemini-3.8-flash-high | 0,2 | ~10 (скрипт PostCSS) | отклонено | Изменений нет: на первом шаге попытался прочитать `C:\Users\khamb` вне проекта, в headless-режиме `read_file` автоматически запрещён, завершился со статусом SUCCESS и пустым ответом. Задачу сделал Claude скриптом PostCSS с проверкой. Для следующих запусков нужно правило `permissions.allow` для `read_file` в папке проекта или `--add-dir` | да |

| 2 | 2026-09-11 | Вынести панели «Помощь», «Экспорт», «Поделиться» из `room-app.tsx` в новый `room-panels.tsx` по точной спецификации пропсов | gemini-3.8-flash-high | 2,0 | ~5 | принято | Нет. Одна неудачная попытка записи (ошибка аргументов инструмента), со второй записал сам. typecheck, 44 теста, lint 0 — сразу; разметка идентична | да |

| 3 | 2026-09-11 | Вынести панели «История», «Таймер», «Голосование» в `room-panels.tsx`; пропсы и типы определить самому (только правила, без готовой спецификации) | gemini-3.8-flash-high | 5,6 | ~8 | принято | Нет. Посимвольная сверка всех трёх панелей прошла; вызовы `act` вынесены в именованные колбэки корректно; typecheck, 44 теста, полный lint 0. Мелочь без правки: пропс назван `s` (как в исходнике) | да |

| 4 | 2026-09-11 | Вынести панели «Инструменты», «Заявки на вход», «Темы», «Действия» в `room-panels.tsx`; пропсы и колбэки (в т.ч. `async` с `try/catch`) определить самому | gemini-3.8-flash-high | 5,7 | ~10 | принято | Нет. Посимвольная сверка всех четырёх панелей прошла, удалено ровно 232 строки (тела блоков); колбэки в `room-app.tsx` совпадают со старым кодом; typecheck, 44 теста, lint 0; панели открываются в браузере | да |

## Итог

Засчитано запусков: 4 из 10 (принято без доработок: 3).

## Замечания по запуску

- Статус `SUCCESS` в выводе `agy` не означает, что работа сделана: проверять `response`, `denied_actions` и сам diff.
- В headless-режиме (`-p`) разрешения не запрашиваются, недостающие автоматически отклоняются.
- Пробы 2026-09-11 (не засчитываются, это не задачи): без правила `permissions.allow` в `~/.gemini/antigravity-cli/settings.json` `agy -p` не может прочитать даже `package.json` в собственном workspace (worktree), хотя workspace определён верно. Читать разрешено только системную временную папку (scratchpad). Кроме того, на задание «прочитай в текущей папке» агент идёт в основной checkout `retro3d`, а не в worktree. Нужны правило `read_file` для папки проекта и абсолютные пути в задании.
- После настройки правил (путь к папке без `**`, см. `CLAUDE.md`) проба прошла: `agy` прочитал `package.json` в worktree и создал в нём файл, 18 с.
